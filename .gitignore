# Python
__pycache__/
*.pyc

# Runtime caches / generated data (regenerate automatically on first run)
seasonality_dynamic.json
pcr_ticker_cache.json
scores_snapshot_*.json

# Legacy / dead (no longer read by the app)
seasonality_all21.json

# Backups & local mockups
*.bak_*
mock_*.html
